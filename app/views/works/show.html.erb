<%= render Element::AlertComponent.new(message: @status_message, variant: :info) %>
<%= link_to 'Edit', edit_work_path(druid: params[:druid]) if @editable %>
<pre class="mt-3"><code><%= CocinaSupport.pretty(cocina_object: @cocina_object.to_h) %></code></pre>
